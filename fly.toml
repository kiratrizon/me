app = "iam-throy"
primary_region = "sin"

[build]
  dockerfile = "Dockerfile"

[env]
  APP_PORT = '6000'
  APP_HOST = '0.0.0.0'

[http_service]
  internal_port = 6000
  force_https = true
  auto_stop_machines = 'off'
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
