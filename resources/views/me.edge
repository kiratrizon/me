<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <!-- SEO -->
  <meta name="description" content="Portfolio of {{ $name }}" />
  <meta name="keywords" content="Portfolio, {{ $name }}, Developer" />
  <meta name="author" content="{{ $name }}" />
  <title>{{ $name }} | Portfolio</title>
  @vite(['resources/ts/app.ts'])
</head>
<body class="bg-gray-950 text-gray-100">

  <!-- ===== NAVBAR ===== -->
  <header class="border-b border-gray-800">
    <nav class="max-w-6xl mx-auto px-6 py-4 flex items-center justify-between">
      <div class="flex items-center gap-4">
        <!-- Profile picture -->
        <img src="/picture/me.png" alt="{{ $name }}" class="w-10 h-10 rounded-full object-cover border-2 border-primary" />
        <h1 class="text-xl font-bold text-white">{{ $name }}</h1>
      </div>

      <!-- Desktop Menu -->
      <ul class="hidden md:flex gap-6 text-gray-300">
        <li><a href="#about" class="hover:text-white">About</a></li>
        <li><a href="#projects" class="hover:text-white">Projects</a></li>
        <li><a href="#skills" class="hover:text-white">Tech Stack</a></li>
        <li><a href="#contact" class="hover:text-white">Contact</a></li>
      </ul>

      <!-- Mobile Hamburger -->
      <div class="md:hidden">
        <button id="menuBtn" class="text-gray-300 focus:outline-none">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
              d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      </div>
    </nav>

    <!-- Mobile Menu -->
    <ul id="mobileMenu" class="hidden flex-col gap-4 bg-gray-950 px-6 py-4 text-gray-300 md:hidden">
      <li><a href="#about" class="hover:text-white block">About</a></li>
      <li><a href="#projects" class="hover:text-white block">Projects</a></li>
      <li><a href="#skills" class="hover:text-white block">Tech Stack</a></li>
      <li><a href="#contact" class="hover:text-white block">Contact</a></li>
    </ul>
  </header>

  <!-- ===== HERO ===== -->
  <section class="max-w-6xl mx-auto px-6 py-24 text-center">
    <h2 class="text-4xl md:text-5xl font-extrabold mb-4">
      Hi, I'm <span class="text-primary">{{ $name }}</span>
    </h2>
    <p class="text-gray-400 max-w-xl mx-auto mb-8">
      Full Stack Developer who builds fast, scalable, and clean applications.
    </p>

    <div class="flex justify-center gap-4">
      <a href="#projects"
        class="px-6 py-3 bg-primary rounded-lg font-medium hover:bg-indigo-500 transition">
        View Projects
      </a>
      <a href="#contact"
        class="px-6 py-3 border border-gray-700 rounded-lg hover:bg-gray-800 transition">
        Contact Me
      </a>
      <a href="/resume" target="_blank"
        class="px-6 py-3 border border-gray-700 rounded-lg hover:bg-green-800 transition">
        Download Resume
      </a>
    </div>
  </section>

  <!-- ===== ABOUT ===== -->
  <section id="about" class="max-w-6xl mx-auto px-6 py-20">
    <h3 class="text-3xl font-bold mb-6">About Me</h3>
    <p class="text-gray-400 leading-relaxed max-w-3xl">
      I am a backend-focused developer specializing in building robust APIs, scalable server-side applications, and efficient backend systems.
      While I have a basic understanding of frontend development, my strength lies in backend architecture, performance optimization, and creating clean, maintainable code.
    </p>
  </section>

  <!-- ===== PROJECTS ===== -->
  <section id="projects" class="bg-gray-900 py-20">
    <div class="max-w-6xl mx-auto px-6">
      <h3 class="text-3xl font-bold mb-10">Projects</h3>

      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
        @each ($project in $projects)
          <div class="bg-gray-950 border border-gray-800 rounded-xl p-6 hover:border-primary transition">
            <h4 class="text-xl font-semibold mb-2">{{ $project.project_name }}</h4>
            <p class="text-gray-400 mb-4">
              {{ $project.description }}
            </p>
            <div class="flex flex-col gap-2">
              <a href="{{ $project.live_demo_url }}" class="text-primary hover:underline" target="_blank">
                View Project →
              </a>
              <a href="{{ $project.github_url }}" class="text-secondary hover:underline" target="_blank">
                View Source Code →
              </a>
            </div>
          </div>
        @end
      </div>
    </div>
  </section>

  <!-- ===== Tech Stack ===== -->
  <section id="skills" class="max-w-6xl mx-auto px-6 py-20">
    <h3 class="text-3xl font-bold mb-8">Tech Stack</h3>

    @each(($items, $category) in $skills)
      <h4 class="text-xl font-semibold mb-4 capitalize">{{ $category }}</h4>
      <div class="flex flex-wrap gap-3 mb-6">
        @each($item in $items)
          <span class="px-4 py-2 bg-gray-800 rounded-full">{{ $item }}</span>
        @end
      </div>
    @end
  </section>

  <!-- ===== CONTACT ===== -->
  <section id="contact" class="bg-gray-900 py-20">
    <div class="max-w-3xl mx-auto px-6">
      <h3 class="text-3xl font-bold mb-8">Contact</h3>

      <form id="contactForm" class="space-y-4">
        <input
          type="text"
          name="name"
          placeholder="Company / Name"
          class="w-full px-4 py-3 bg-gray-950 border border-gray-800 rounded-lg focus:outline-none focus:border-primary"
          required
        />
        <input
          type="email"
          name="email"
          placeholder="Email"
          class="w-full px-4 py-3 bg-gray-950 border border-gray-800 rounded-lg focus:outline-none focus:border-primary"
          required
        />
        <textarea
          name="message"
          placeholder="Message"
          rows="4"
          class="w-full px-4 py-3 bg-gray-950 border border-gray-800 rounded-lg focus:outline-none focus:border-primary"
          required
        ></textarea>

        <button
          type="submit"
          class="w-full py-3 bg-primary rounded-lg font-medium hover:bg-indigo-500 transition"
        >
          Send Message
        </button>

        <!-- Success / Error message -->
        <p id="formMessage" class="mt-4 text-sm font-medium hidden"></p>
      </form>
    </div>
  </section>

  <!-- ===== FOOTER ===== -->
  <footer class="border-t border-gray-800 py-6 text-center text-gray-500">
    © {{ new Date().getFullYear() }} {{ $name }}. All rights reserved.
  </footer>

  <script>
    // Mobile menu toggle
    const menuBtn = document.getElementById('menuBtn');
    const mobileMenu = document.getElementById('mobileMenu');

    menuBtn.addEventListener('click', () => {
      mobileMenu.classList.toggle('hidden');
    });
  </script>
</body>
</html>
